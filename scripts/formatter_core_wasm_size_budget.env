# Formatter-core wasm artifact size budget, based on the current
# `scripts/measure_formatter_core_wasm_size.sh` baseline with a small allowance
# for deterministic growth while keeping regressions visible in CI.
RAW_BYTES_MAX=40000
GZIP_BYTES_MAX=18000
